name: constraint-gate
version: 0.9.2
description: "Pre-response constraint scanner — enforces configurable rules (language ratio, banned patterns, interaction style) on every assistant response before delivery. Plugs into the transform_llm_output hook."
author: "Hermes Agent (universal plugin)"
hooks:
  - transform_llm_output
